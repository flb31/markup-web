title
    = title
meta(
  charset="utf-8"
)
meta(
  content=""
  name="description"
)
meta(
  content="initial-scale=1.0,user-scalable=no,maximum-scale=1,width=device-width"
  name="viewport"
)
link(
  rel="stylesheet"
  href="assets/css/main.css?r=" + rand
)
meta(
  name="theme-color"
  content="#{metaColor}"
)
meta(
  name="apple-mobile-web-app-status-bar-style"
  content="#{metaColor}"
)
include sign.html
